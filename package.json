{
  "name": "tms-unbound-006",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "deploy:bundle": "turbo build && rm -rf apps/backend/public/build && cp -r apps/web/dist apps/backend/public/build",
    "deploy:phone": "ssh -t apps@192.168.1.80 \"cd ~/apps/tms.zephy.co.in && git pull && pnpm deploy:bundle && pm2 start ~/ecosystem.config.js\"",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "turbo": "^2.9.15",
    "typescript": "~6"
  },
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=20"
  }
}
